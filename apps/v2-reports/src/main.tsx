import React from 'react';
import ReactDOM from 'react-dom/client';
import GuardianReportsPage from './pages/index';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <GuardianReportsPage />
  </React.StrictMode>,
);
